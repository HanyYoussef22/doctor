import 'package:flutter/material.dart';

class PasswordAndEmail extends StatelessWidget {
  const PasswordAndEmail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        
      ],
    );
  }
}